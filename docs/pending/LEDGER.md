# Pending-items ledger

Every pending item found in this repo — TODO/FIXME markers, unexecuted audit and review plan steps,
doc open questions, open GitHub issues, and recorded-but-unacted Claude memory — plus the decision the
user made about it.

Maintained by **`/wow-addon:pending-audit`**. The command re-sweeps the repo on every run and matches
what it finds against the rows below on **ID + evidence hash** (the first 8 hex chars of `sha1` over
the item's verbatim evidence text). A row whose evidence has since changed no longer matches, so the
item correctly re-surfaces for a fresh decision.

Do not hand-edit rows for items you are not deciding on — the command merges, it never clobbers.

## Notation

| Marker | Value | Meaning | Re-surfaces? |
|---|---|---|---|
| 🟢 | `done` | Implemented this run | No — closed |
| 🔵 | `wont-do` | User decided it will never be done | No — closed |
| 🟡 | `deferred` | Not now; still on the books | Yes, as a collapsed count |

Both the marker and the word are always written: the word is the data (greppable, screen-reader safe),
the marker is the affordance. There is deliberately no red — nothing here is an error state.

## Decisions

| ID | Evidence hash | Source | Decision | Date | Rationale |
|---|---|---|---|---|---|
| DOC-02 | `5c098496` | `docs/audits/2026-07-18/06_EXECUTION_OUTCOME.md` (LH-17) | 🟢 done | 2026-07-31 | "Make this consistent with WowAddonStandards and other Ka0s addons" — `.luacheckrc` now carries the `lint.md` template line verbatim, matching BankLedger/PanelMaster. |
| DOC-03 | `792cf11c` | `docs/audits/2026-07-18/06_EXECUTION_OUTCOME.md` §3 | 🟢 done | 2026-07-31 | User chose "File a GitHub issue upstream" over patching the standard locally. Filed as WowAddonStandards#1; the `toc-file-§5` ↔ `layout-§1` conflict is now tracked at the source. |
| DOC-01 | `51e54f78` | `docs/audits/2026-07-18/06_EXECUTION_OUTCOME.md` (LH-16) | 🟢 done | 2026-07-31 | Premise void — `tools/` no longer exists (removed with Export-to-AI, commit `10f3851`) and `.pkgmeta`'s `ignore:` already matches the `packaging.md` template exactly. No change needed; not put to the user. |
| DOC-04 | `f3f0a3f1` | `docs/audits/2026-07-18/06_EXECUTION_OUTCOME.md` §5 | 🟢 done | 2026-07-31 | "Add to docs/smoke-tests.md" — the reordered-TOC clean-load check now lives in §1 as a numbered regression step rather than only inside a frozen audit bundle. |
| MEM-01 | `ca050797` | memory `currency-capture-branch-pending-merge.md` | 🟢 done | 2026-07-31 | "Mark this as done, all smoke tests have been verified" — all 7 owed currency in-game smokes confirmed passing; memory entry rewritten as a design record. |
| MEM-02 | `b442b733` | memory `ah-price-branch-pending-merge.md` | 🟢 done | 2026-07-31 | "Mark verified — done" — AH-price in-game smokes confirmed passing, including the load-bearing AH-page freeze fix; memory entry rewritten as a design record. |
| MEM-03 | `a64c7bf6` | memory `currency-capture-branch-pending-merge.md` | 🟢 done | 2026-07-31 | "Rewrite both memory entries" — stale claims corrected: no `TODO(currency-ai)` exists, Export-to-AI was removed 2026-07-26, both branches merged, smokes passed. |
| DOC-05 | `098550b4` | `docs/ARCHITECTURE.md:311` | 🟢 done | 2026-07-31 | "Resolve the contradiction in docs" — ARCHITECTURE.md no longer calls the single-slot TTL a backlog item; it now points at scope.md, which already settles it as a resolved design decision. The limitation itself stays documented. |
| DOC-07 | `3419df05` | `docs/module-map.md:109` | 🔵 wont-do | 2026-07-31 | User closed the serialized "v2 export" permanently — the addon is CSV-only since Export-to-AI was removed. Follow-through accepted: `module-map.md` and `schema.md` reworded from "deferred to the v2 export" to statements of the settled position, so neither reads as intent-to-change. LibSerialize/LibDeflate are not vendored and will not be. |
| DOC-06 | `e1b0ed85` | `docs/ARCHITECTURE.md:305`, `docs/scope.md` | 🟡 deferred | 2026-07-31 | Needs a real group-loot roll win to confirm the `LOOT_ROLL_YOU_WON` assumption. Costs nothing to wait — the caveat is honestly documented, §F-009 is written up, and the failure mode is a wrong-but-harmless source, never an error. |
| ISS-16 | `f845255b` | GitHub issue #16 | 🟡 deferred | 2026-07-31 | Item-returning refunds on `CHAT_MSG_LOOT` remain speculative-but-retained; the confirm-or-remove call waits. Issue #16 stays open. |
| CODE-01 | `d01fce77` | `modules/Browser.lua:14` | 🟡 deferred | 2026-07-31 | The marker's own condition (post-1.0.0) has not been reached and it names where it is tracked. Same item as ISS-11. |
| ISS-11 | `acdd09b7` | GitHub issue #11 | 🟡 deferred | 2026-07-31 | Configurable window styling — real feature work, backlog. Same item as CODE-01; issue stays open. |
| ISS-09 | `95cc66bc` | GitHub issue #9 | 🟡 deferred | 2026-07-31 | Column chooser — ordinary unbuilt backlog, not unfinished work. Would require making the fixed column geometry (and its numeric test asserts) dynamic. Issue stays open. |

## LibKa0s adoption (2026-08-01)

The decisions taken while adopting the shared [LibKa0s](https://github.com/tusharsaxena/LibKa0s)
library, one row per call, written when the call was made. A **declined** surface is recorded for
the same reason an adopted one is: an unrecorded decision is indistinguishable from an oversight,
and the next consistency sweep would "fix" it. Naming the upstream minor that unblocked a row is
what makes "why now?" answerable later.

**`LIBKA0S-17` is now ratified as a register row**, not only a ledger decline: the
`performance-§12` no-combat-path exemption lives in
[`../ARCHITECTURE.md` § Documented deviations](../ARCHITECTURE.md#documented-deviations) with its
committed sweep in [`../performance.md`](../performance.md). A decline reasoned only here is not an
exemption. **Two obligations survive it**, and both are easy to drop by accident: `libs/LibKa0s/` is
still vendored whole (`Perf.lua` included), and the **next release notes must carry the
`perf: skip` sentence naming this exemption** (`automated-tests-§3`) rather than letting the skip
read as measured.

| ID | Evidence hash | Source | Decision | Date | Rationale |
|---|---|---|---|---|---|
| LIBKA0S-01 | `29e9751b` | `core/CoreSetup.lua`, `core/Util.lua` | 🟢 done | 2026-08-01 | `LibKa0s-Core-1.0`'s printer and secret-safe stringifier adopted; the ~37-line block that owned them in `core/Util.lua` deleted. A pure refactor at the seam — this addon's guard was already the `table.concat` probe with the same `<secret>` sentinel the library ships, so not one rendered byte moves. `NS.Print` and `NS.Util.print` are BOTH published, because `core/LootHistory.lua`'s AceConsole reclaim reads the latter back over the former; a seam publishing only `NS.Print` is silently undone by that reclaim while appearing to work. |
| LIBKA0S-02 | `1f034b40` | `modules/Browser.lua` (`B:ApplySkin`), `libs/LibKa0s/Core.lua` (`SKIN`) | 🔵 wont-do | 2026-08-01 | **Superseded by LIBKA0S-18 on 2026-08-02 for the skin half; still current for the close button.** | Core's window chrome (`SKIN` / `ApplySkin` / `MakeCloseButton`) **declined**. This addon draws every window with a flat 1px `WHITE8X8` double border, a synthesized inner-border child frame, a gold title tint and a 24x24 class-colored × glyph; Core's is a 12px `UI-Tooltip-Border` and an 18x18 fixed-red ×. Adopting it would redesign the History browser, which is not a library migration. The console keeps this addon's chrome through DebugLog minor 4's `applySkin` / `makeCloseButton` hooks (LIBKA0S-05), so nothing drifts. |
| LIBKA0S-03 | `246dd001` | `tests/run.lua`, `tests/wow_mock.lua`, `tests/_kit/` | 🟢 done | 2026-08-01 | The shared test kit adopted: `tests/loader.lua` deleted, `tests/run.lua` moved onto `Kit.expose`/`Kit.run`, and `tests/wow_mock.lua` reduced to an extender over `mock_base.lua`. Required rather than cosmetic — the old mock's LibStub had **no `NewLibrary`**, so no vendored LibKa0s file could have registered at all. Three behaviors the base models that the old mock did not now run for real: the AceConsole `:Print` clobber (which makes the `NS.Print` reclaim a live assertion instead of a vacuous one), a fireable AceGUI widget factory, and `Settings.RegisterCanvasLayoutSubcategory`. `NewAddon` is wrapped locally to embed the AceEvent bus, which the real mixin list does and the base fake deliberately leaves to the host. |
| LIBKA0S-04 | `ded3f2ac` | `core/DebugLogSetup.lua`, deleted `modules/DebugLog.lua` | 🟢 done | 2026-08-01 | `LibKa0s-DebugLog-1.0` adopted whole; `modules/DebugLog.lua` (359 lines) deleted outright. Both formatters were already byte-identical to the library's, the frame globals the descriptor generates from `name` are the exact names that file hardcoded, and the buffer, the 500-line cap, the `N / MAX lines` counter and the color-coded enable ack all match to the byte — all 16 pre-existing cases in `tests/test_debuglog.lua` pass unmodified against the library, which is the parity proof. Gained: the copy scroll now carries a global name (`UIPanelScrollFrameTemplate` derives its scrollbar children's names from the parent's, so the old anonymous one left them unstyled), `Add` no longer requires a frame, and `onVisibilityChanged` fires on OnHide too — so closing the console with Esc or the × now syncs the settings checkbox, which it never did. |
| LIBKA0S-05 | `4d4d4dcd` | `core/DebugLogSetup.lua` (`applySkin`, `makeCloseButton`) | 🟢 done | 2026-08-01 | The two DebugLog **minor 4** hooks are what made LIBKA0S-04 possible: a `skin` table reaches only the three backdrop calls, and this addon's chrome also has a synthesized inner-border child frame, a gold title tint and a gray divider tint — calls, not fields. Both are passed as CLOSURES resolving `NS.Browser` at frame-build time, never as load-time locals: `core/DebugLogSetup.lua` loads long before `modules/Browser.lua`, and hoisting either lookup would silently lose the skin. This addon is the **second** host on both hooks (BankLedger was the first and only), so the width-derivation half is now asserted rather than assumed — see LIBKA0S-06. |
| LIBKA0S-06 | `4232993d` | `tests/wow_mock.lua` | 🟢 done | 2026-08-01 | The kit's frame stub answers `0` from `GetWidth` forever, which made the one part of `makeCloseButton`'s contract that matters to this host unobservable: the library DERIVES the console's Copy/Clear title-bar offsets from the returned button's width, and this addon's × is 24 wide where Core's is 18. Extended locally (never in `tests/_kit/`) with a size-recording frame factory, so the offsets assert at −36 / −84 rather than the −30 / −78 a Core-width button gives. Safe here because no suite in this repo spies on a frame setter, which is the reason the kit leaves them undefined. Not raised upstream: one host, one need, and the fix is four lines in the host's own extender. |
| LIBKA0S-07 | `a2824aed` | `settings/Slash.lua`, `settings/Schema.lua`, `core/Constants.lua` | 🟢 done | 2026-08-01 | `LibKa0s-Slash-1.0` adopted for the dispatcher, the help renderer, the landing rows, the schema CLI and the type-aware parser; `settings/Slash.lua` drops from 263 to 241 lines and most of what remains is the confirm popups and the total-reset composition, which were never the library's. Two mechanical prerequisites moved with it, and neither would have failed loudly: `NS.COMMANDS` went from keyed `{name=, desc=, fn=}` to the positional `{ name, description, handler }` triples the library reads, and four schema-row field names became LibKa0s's (`type = "boolean"` → `"bool"`, `options` → `values` with each entry's `label` → `text`, `soloRow` → `solo`, `panelSkip` → `skipRender`). A row that kept the old spelling does not error — it silently vanishes from its page and answers ERR_TYPE on every `set`. `tooltip` deliberately did NOT move: the library reads `tooltip` first and its own `desc` second. |
| LIBKA0S-08 | `38c819dc` | `settings/Slash.lua` (`format`), `libs/LibKa0s/Slash.lua:364` | 🟢 done | 2026-08-01 | The two set-valued rows (`settings.excludedSources`, `settings.auction.capture`) carry `type = "table"`, which is not one of the library's four types — `lib.FormatValue` falls through to Core's `SafeToString`, which probes `table.concat`, fails, and answers `<secret>`, telling the user a plain settings value is combat-protected. Slash **minor 5**'s `format` hook is the supported answer (it exists for BankLedger's muted-store set), so `Sl.FormatSchemaValue` keeps only the `table` branch and hands everything else straight back to `lib.FormatValue`. Not worked around host-side, and not a new library gap. |
| LIBKA0S-09 | `781f61d1` | `settings/Panel.lua` (`buildMainContent`), `settings/Slash.lua` (`Sl:LandingRows`) | 🟢 done | 2026-08-01 | **Convergence #2 — ADOPTED.** The settings landing page carried its own command-row formatter (`\|cffffff00/lh %s\|r  \|cffffffff—\|r  %s` — double spaces, the dash white-wrapped, the description bare) while `settings/Slash.lua` two files away rendered the same data another way. Both now go through `lib.FormatRow` via `Sl:LandingRows()`. User-visible: the double spaces collapse to single, the dash loses its color span, and the description gains one. That is the accepted cost — it removes a silent drift that existed in this repo for as long as both files have. |
| LIBKA0S-10 | `50f9f632` | `settings/Slash.lua` (`CliReset`), `NS.COMMANDS` | 🟢 done | 2026-08-01 | **Convergence #1 — ADOPTED, at no cost.** `/lh reset` was ALREADY path-scoped here and `/lh resetall` already existed, so unlike KickCD, prettychat, ConsumableMaster and WhatGroup this addon loses no capability and needs no re-anchored confirmation popup: the destructive verb is `/lh purge` (and the panel's "Reset All"), both of which still route through `StaticPopup_Show` untouched. Both entry points are in `docs/smoke-tests.md`. The echo wording changed — see LIBKA0S-12. |
| LIBKA0S-11 | `d04e1721` | `settings/Slash.lua` (`Sl:CliResetAll`) | 🟢 done | 2026-08-01 | `CliResetAll` WRAPS the library verb rather than re-exporting it. The two item-id filter lists and the currency blacklist are user-configured settings with no schema row (a ratified storage carve-out managed by `NS.Filters`), so the library's row walk cannot see them. They are cleared first, so the library's acknowledgment is the last line printed and reads as the summary of everything that happened. Not a library gap: a `beforeResetAll` hook would serve exactly one host. |
| LIBKA0S-12 | `2a13ac39` | `tests/test_slash.lua` | 🟢 done | 2026-08-01 | The rendered strings that deliberately changed, each pinned by a case that names the change: command rows and `key = value` pairs now use the library's UPPER-case hex (`\|cFFFFFF00`) where this addon used lower; the chat help header gains an em dash (`v1.2.0 — slash commands`) and its rows a two-space indent; `reset` echoes the shared `FormatKV` line instead of a bespoke `<path> reset to <value>` sentence; `resetall` acknowledges with a capital A. Gained rather than changed: `set` now clamps a number to its slider range and echoes what was actually stored, refuses a value outside a numeric enum instead of storing it, and refuses an unreadable boolean instead of silently storing `false`. |
| LIBKA0S-13 | `d7df1267` | `settings/OptionsSetup.lua`, `settings/Panel.lua` | 🟢 done | 2026-08-01 | `LibKa0s-Options-1.0` adopted: the canvas shell, the breadcrumb header, the lazy Defaults button, the page registry, `EnsureScroll` + the always-shown scrollbar patch, `Section`, `AddSpacer`, `AttachTooltip`, `RenderField`/`RenderRows`/`RenderSchema`, `RestoreDefaults`, `RefreshScalars`/`RefreshAllPanels`, `SetRenderer` and `OpenOptionsPanel`. `settings/Panel.lua` drops 1034 → 785 lines against a new 114-line setup file, a net −135. It was a migration rather than a redesign because every layout constant already matched to the number (PADDING_X 16, HEADER_TOP 20, HEADER_HEIGHT 54, DEFAULTS_W 110, ROW_VSPACER 8, the 10/6/26 section triple, BUTTON_PAIR_REL 0.492) and the breadcrumb separator was byte-identical. Gained: the main canvas is now a NAMED frame (`LootHistorySettingsPanel`, so `/framestack` attributes it and two addons cannot collide), the Blizzard Settings window's own footer Defaults control now works (`CreatePanel` stamps `OnDefault` — every host on this module shipped that dead), each row is pcall-guarded so one corrupt saved value costs a row rather than the page, the combat guard now also fires when the AddOns sidebar reaches a page directly, and `Refresh` now re-syncs every page rather than only General. |
| LIBKA0S-14 | `3b48a03a` | `settings/Panel.lua` (`makeMultiCheck`, `AFTER_GROUP`) | 🔵 wont-do | 2026-08-01 | The inverted set picker is **declined** — the library's makers are checkbox / slider / dropdown / editbox / color picker, and a wrapping InlineGroup of checkboxes whose stored value is the logical INVERSE of the tick is none of them. `RenderGrid` is not the answer either: it takes no `parent`, renders flush into the whole of `ctx.body`, and would create a second overlapping scroll frame (the gap tracked at KickCD#10). The rows carry `skipRender`, so the schema, the CLI and every reset still see them, and the picker is drawn from `afterGroup`, which fires after its group's last row is flushed — i.e. exactly where the generic path used to put it. Not raised upstream: one host, one shape, and a fifth maker for it would have exactly one consumer. |
| LIBKA0S-15 | `bea7069c` | `settings/Panel.lua` (the AH Price builder), `docs/settings-panel.md` | 🔵 wont-do | 2026-08-01 | `SetRenderer` is **declined on the AH Price page alone**, and the reason is structural rather than stylistic. The library's renderer contract is "re-run me and I redraw the page", which means a `ClearScroll` releasing every AceGUI child back to the pool. That page parents eleven reusable row slots of raw FontStrings and Buttons to an AceGUI SimpleGroup's frame, created once and repainted in place; releasing the group would orphan them. The ~213-frame version of this page froze the client ~1.7s on tab-transition and the pooling is what fixed it. The page keeps its own OnShow and calls `O.EnsureDefaultsButton` itself; everything else on it is still the library's. The other three pages all go through `SetRenderer`. |
| LIBKA0S-16 | `a0bd0ebf` | `settings/OptionsSetup.lua` (`buildMain`), `settings/Panel.lua` (`P.BuildMain`) | 🟢 done | 2026-08-01 | The landing page moves onto the descriptor's `buildMain`, late-bound through `NS.Panel` because `settings/Panel.lua` takes `NS.Options` as a file-scope upvalue and therefore cannot load first. The library routes it through `SetRenderer`, so the main page gains the lazy first-show render, the combat guard and the dirty-re-render — it had none of the three, and its body was drawn from a bespoke `mainRendered` flag. |
| LIBKA0S-17 | `34d1776d` | `modules/Collector.lua:213`, `modules/Attribution.lua:332`, whole-repo `OnUpdate` sweep | 🔵 wont-do | 2026-08-01 | `LibKa0s-Perf-1.0` is **declined**, not deferred, for two independent reasons. **There is no hot path.** The addon registers eleven events and owns no `OnUpdate`, no repeating ticker and no repaint loop — a whole-repo sweep finds exactly three `C_Timer.After` calls, all one-shot (a 5s login prune, a 0.4s item-cache retry, and the color-drag throttle the library asked for). The only per-event work of any size is one `CHAT_MSG_LOOT` line, which fires a few times per fight, not a few times per frame; a bucket over it would report a number too small and too rare to compare, which is the "a path that runs twice a session is not a bucket" case. **And `suspend` would destroy user data.** The probe's windows are combat-gated and `suspend` must make the host inert for the whole of window B — for this addon that means not recording the loot that drops during that fight, so running an experiment would silently cost the user real history. A diagnostic that damages the thing it measures is worse than no diagnostic. Same shape of decline as BankLedger's own LIBKA0S-17, reached from a different direction: theirs would read 0.000 by construction, this one would read almost nothing AND take history with it. `LootHistoryPerfDB` is deliberately NOT declared in the TOC. |
| LIBKA0S-18 | `6e0181ed` | `libs/LibKa0s/Core.lua` (Core minor 3), WowAddonStandards v2.16.0 | 🟢 done | 2026-08-02 | **LIBKA0S-02's skin half is void: `Core.SKIN` now IS this treatment.** The in-game smoke pass put all five adopters' debug consoles on screen at once and they split into two looks — this addon and BankLedger drawing a flat 1px black edge with a 1px gray inner highlight, a gold title and a gray divider, and AbsorbTracker / ConsumableMaster / KickCD on the library's 12px `UI-Tooltip-Border` with a black divider. Two of the five had overridden the shared seam in order to look right, which is the shared seam failing at the thing it exists for. Fixed **upstream, not here**: `standalone-windows` in the Ka0s WoW Addon Standard now specifies the edge normatively, and LibKa0s v1.3.0 (Core minor 3, DebugLog minor 5) makes `Core.SKIN` carry it and `Core.ApplySkin` draw all of it. Nothing in this addon changed appearance; the three siblings did. The `applySkin` hook (LIBKA0S-05) stays, with its purpose narrowed from "rescue the console from a default that does not match" to "keep the console tracking `modules/Browser.lua`'s own re-skin seam" — one of the two purposes the standard still sanctions. `Core.MakeCloseButton` remains declined: 18x18 fixed-red against this addon's 24x24 class-colored glyph. |
| LIBKA0S-19 | `1dd31fcc` | `core/DebugLogSetup.lua`, `libs/LibKa0s/DebugLog.lua` (minor 6) | 🟢 done | 2026-08-02 | **The `makeCloseButton` hook is dropped: the console and the copy window wear Core's ×, not this addon's.** Same root cause as LIBKA0S-18, one descriptor field along. The hook was taken on the reasoning that all of this addon's windows should close alike — which produced two diagnostic windows that matched this addon and no other, while the three siblings that passed nothing wore Core's thin 18Ý18 ×. The split the standard now draws (`standalone-windows`): the window **edge** is shared across every Ka0s window, but the **close control on a library-drawn window is the library's**. The History browser keeps its 24Ý24 class-colored glyph — that window is this addon's and was never in question. `applySkin` (LIBKA0S-05) stays: it is the opposite case, a host seam the library's window legitimately tracks. Fixed upstream too — LibKa0s v1.3.1 documents the field as rarely-right and now-unused, and carries a regression guard that a console built without it reaches `Core.MakeCloseButton` exactly twice. |
