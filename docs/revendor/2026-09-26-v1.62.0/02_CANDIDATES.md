# Candidates (LootHistory)

None. v1.62.0 is the library's half of the 2026-09-26 automated-tests sweep: four peels
(OptionsRegistry, OptionsIds, OptionsIdList, OptionsCombat) and kit revisions 28 to 31. It adds no
public member, descriptor field or row field.

Delivered on the re-vendor alone (class A):

- The runner's `RESULTS.md` prints `None.` under an empty watch-list table (ATS-20, kit revision 30).
- The runner's band table leaves out generated files named by `Kit.layoutCap.exempt` (ATS-21, kit
  revision 31). LootHistory declares no such set, so nothing it reports changes.
