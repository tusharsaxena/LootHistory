# Module map

Where each responsibility lives in the source tree, plus the TOC load order and the AceAddon lifecycle hooks. `LootHistory.toc` is the source of truth for load order — match this map to it before editing. 23 source files across `core/`, `defaults/`, `locales/`, `settings/`, `modules/`; the internal-only terms **Collector** (capture) and **Browser** (view) are used throughout.

## Directory tree

```
Ka0s Loot History (AceAddon: NS promoted in place)
├── core/
│   ├── Compat.lua      — LOADS FIRST. Every deprecated/flavor-varying API shim behind
│                          a C_*/global presence check: GUID decode, item info/extras,
│                          bound-scan, map id, keystone, mail-header/AH detection, loot
│                          global-strings, spell name. Retail-only; degrades to nil/false.
│   ├── MediaSetup.lua   — the LibKa0s-Media-1.0 seam: NS.Icon / NS.MediaFont /
│                          NS.IconMarkup, and the LibSharedMedia registration, at
│                          FILE LOAD. LOAD-BEARING TOC POSITION: above Constants.lua.
│   ├── Constants.lua    — NS.Constants: SourceType enum (the export
│                          contract — stable string keys), SourceOrder/SourceLabel,
│                          SOURCE_IMPLEMENTED gate, quality/retention/source option
│                          tables, ITEMCLASS_QUEST, CONTEXT_TTL, FONT_MONO_NAME/FONT_MONO.
│   ├── Namespace.lua    — bootstrap metadata: NS.name, NS.version ("1.2.0"), NS.PREFIX.
│   ├── State.lua        — NS.State: runtime-only mutable state (lootContext, encounter,
│                          keystone, cleanupDone, debug, testRecords). Never persisted.
│   ├── Util.lua         — pure helpers: PlayerKey, SplitPath, date/money/byte formatters,
│                          RangeFrom (shared date-range→epoch), CHAT_MSG_LOOT self-parse.
│   ├── CoreSetup.lua    — LibKa0s-Core-1.0 seam: NS.Print / NS.Format / NS.Util.print /
│                          NS.SafeToString / NS.IsConcatSafe. Also publishes
│                          NS.LIBKA0S_MISSING, the shared cause clause every other
│                          LibKa0s seam appends its own consequence to.
│   ├── WidgetsSetup.lua — LibKa0s-Widgets-1.0 seam: NS.MakeDropdown (one factory for all
│                          ten dropdowns, art resolved here and passed as parameters),
│                          NS.HasWidgets (availability asked with nothing built),
│                          NS.CloseMenu (the process-wide popup no frame's Hide can reach)
│                          and NS.CopyWindow (the export copy window, built lazily).
│   ├── DebugLogSetup.lua— LibKa0s-DebugLog-1.0 seam: NS.DebugLog + the NS.Debug sink.
│                          Passes this addon's own window edge through applySkin;
│                          makeCloseButton deliberately NOT passed (DebugLog minor 4).
│   ├── LootHistory.lua  — AceAddon:NewAddon(NS,…); NS.addon / NS.bus / NS.NewBusTarget;
│                          OnInitialize / OnEnable / OnEnterWorld lifecycle.
│   └── Database.lua      — AceDB init + migration seam; ActiveHistory (test-mode swap over the raw
│                          history — filtering is point-in-time, decided at capture) / Add / QueryList /
│                          Query / Export / Stats / Delete / Purge / PruneOld / StorageStats /
│                          FireHistoryChanged. Fires RecordAdded / HistoryChanged.
├── defaults/
│   └── Global.lua       — NS.defaults.global: schemaVersion, history[], blacklist{}, whitelist{},
│                          currencyBlacklist{}, settings{} (incl. recordCurrency + the auction cascade),
│                          minimap{}. Account-wide (.global, not .profile).
├── locales/
│   └── enUS.lua         — NS.L with a key-returning metatable fallback. v1 ships
│                          English-only; no string routes through NS.L yet (seam kept).
├── settings/
│   ├── Schema.lua       — one row per setting; S:Set single write seam; boot validation;
│                          NS.COMMANDS slash dispatch table.
│   ├── Slash.lua        — LibKa0s-Slash-1.0 seam: AceConsole /lh + /loothistory binding,
│                          the dispatcher, help + landing rows, the schema CLI and the
│                          type-aware parser. Keeps the purge/reset confirm popups, the
│                          total-reset composition and the set-valued `format` hook.
│   ├── OptionsSetup.lua — LibKa0s-Options-1.0 seam: the canvas shell, page registry,
│                          widget makers and flow engine. Where every declined surface
│                          is written down.
│   └── Panel.lua        — Settings.RegisterCanvasLayoutCategory landing page + General /
│                          Filters / AH Price subcategories; lazy schema render; History stats.
└── modules/
    ├── Attribution.lua  — source-resolution engine: stamps a short-lived loot context
                           from peripheral events/hooks, consumes it on CHAT_MSG_LOOT.
                           Loads BEFORE Collector.
    ├── Filters.lua      — NS.Filters: blacklist/whitelist item-id lists + the currencyID-keyed
                           currency blacklist (add/remove, parse id from link, notify). Loads before
                           Collector. Data-only, no Enable.
    ├── AuctionPrice.lua — NS.AuctionPrice: GatherAll(itemLink, itemID) captures every enabled
                           provider:key price into a nested map; Pick(map) resolves one at read
                           time via the settings.auction.priority cascade. Installed pricing addons
                           (Auctionator/TSM/OribosExchange). Third-party integration boundary —
                           presence-gated, deliberately outside core/Compat.lua. Loads before Collector.
    ├── Collector.lua     — CHAT_MSG_LOOT handler: self-filter → whitelist/blacklist + quality/
                           source/quest gate → AuctionPrice:GatherAll → build record →
                           Database:Add. Also a CHAT_MSG_CURRENCY handler (recordCurrency toggle +
                           source mute + currency blacklist → Type=Currency row). Hot-path upvalues.
    ├── Browser.lua       — standalone-windows window shell: frame, skin, tabs, SHARED filter bar +
                           footer (nine LibKa0s-Widgets-1.0 dropdowns via NS.MakeDropdown), saved
                           view, tab-aware Export, minimap (LDB+LibDBIcon). Bus sub.
    ├── BrowserTable.lua  — virtualized pooled-row History table: filter→group→sort→slice
                           →bind; column model; row menu (link/blacklist/delete); /lh test data.
    ├── Export.lua        — export modal (NS.Export:Open): config-driven Data Set dropdown
                           (NS.MakeDropdown); CSV
                           (rows) + InsightsCSV (analytics); Wowhead links; the library's copy window.
    └── Analytics.lua     — Insights tab: LOOT + CURRENCY section blocks — stat cards + breakdown
                           charts + top lists off a single Database:Stats pass scoped by the SHARED
                           filter; pooled widgets.
```

See [schema.md](schema.md) for the record shape, [message-bus.md](message-bus.md) for the closed bus, [slash-dispatch.md](slash-dispatch.md) for the command table, [settings-panel.md](settings-panel.md) for the canvas panel, [compat-layer.md](compat-layer.md) for the API firewall, [data-flow.md](data-flow.md) for source resolution, and [browser.md](browser.md) for the window.

## TOC load order

`LootHistory.toc` orders files by dependency, not alphabetically. Libraries first, then `locales/`, `core/` (Compat first), defaults, modules (Attribution and Filters before Collector), and settings last.

1. `libs/` — vendored LibStub, CallbackHandler-1.0, AceAddon-3.0, AceEvent-3.0, AceTimer-3.0, AceConsole-3.0, AceDB-3.0, AceGUI-3.0, LibSharedMedia-3.0, LibDataBroker-1.1, LibDBIcon-1.0, and **LibKa0s** (`libs\LibKa0s\LibKa0s.xml`, last in the block — nine of its ten majors resolve `LibKa0s-Core-1.0` before registering (every one but Core itself)). Committed in-tree (Ka0s Standard v2.0.0 — vendoring is mandatory; `.pkgmeta` externals are forbidden). Don't edit.
2. `locales/enUS.lua` — `NS.L` with a metatable that returns the key itself, so English strings work untranslated and missing keys never error. The addon ships English-only: no user-facing string routes through `NS.L` yet (an accepted scope decision, `enUS.lua:7-11`); the seam is kept for a future localization pass. Loads early (before `core/`) so `NS.L` exists for everything after it.
3. `core/Compat.lua` — **loads first** of `core/`. Hangs `NS.Compat` (the private namespace `NS` is the second file vararg, not a global) and populates every deprecated/varying API shim: `GetActiveKeystoneLevel`, `HookUseContainerItem` / `ContainerItemHasLoot`, `HookGetQuestReward` / `CurrentQuestID`, `IsSpellTargeting`, `GetSpellName`, `GetMailHeader` / `IsAuctionHouseMail`, `DecodeGUID` (+ `UNIT_KINDS`), `GetItemInfo`, `ItemNameQuality`, `ScanBound`, `BindState` / `ItemBindState` / `BestBound` (the two warbound signals and their merge), `GetItemExtras`. Each is gated by a direct `C_*`/global presence check (`Compat.lua:5-7`) — no `WOW_PROJECT_ID` branching — so anything later can rely on `NS.Compat` existing. The map, zone and TOC-metadata shims it used to carry are gone: they behaved identically in every addon that had them, so they are `LibKa0s-Env-1.0`'s now and reach this addon through `core/EnvSetup.lua` (4 below). Three item primitives went the same way — `QualityFromLink`, `QualityLabel` and `LoadItem` are `LibKa0s-Item-1.0`'s and reach this addon through `core/ItemSetup.lua`. **`GetItemInfo` and `ItemNameQuality` stayed**, deliberately: guessing for an uncached item is this addon's policy, opposite to BankLedger's refusal, and only the primitives under the guess were shared.
4. `core/EnvSetup.lua` — the **`LibKa0s-Env-1.0`** seam, and the file that took over three of `core/Compat.lua`'s shims. Publishes `NS.Meta(field)` (one TOC manifest field, `nil` when the field is absent or the client exposes no reader), `NS.Version()` (the TOC version, falling back to `NS.version` and then `"?"`, so `/lh version` can never answer nil), `NS.PlayerMapID()` and `NS.Zone()`. The library is handed `addonName` — the addon **folder** name, the first vararg, a different question from `## Title` — because LibKa0s is vendored and a copy cannot know which folder it sits in. TOC position is **conventional, not load-bearing**: nothing resolves here beyond the LibStub lookup, and `NS.version` is read at call time because `core/Namespace.lua` loads after this file. Every helper writes its pre-library ladder out in full, so a degraded install still reads its own TOC and still stamps its own zone. `NS.Zone` answers **two strings and never nil** — `""` buckets with nil on purpose in `Database:Stats`, in the Zone filter and in the table's group-by-zone, and a nil here would move stored rows between buckets.
5. `core/MediaSetup.lua` — the **`LibKa0s-Media-1.0`** seam, and the one file in `core/` whose TOC position is load-bearing rather than conventional: it must sit **above `core/Constants.lua`**, which resolves `FONT_MONO` from `NS.MediaFont` at file load. Publishes `NS.Icon(name)` (the extensionless texture path for one catalog mark, `nil` when the library is absent or the name is not one it ships), `NS.MediaFont(name)` (the path of one shipped face, `nil` likewise) and `NS.IconMarkup(name, fallback, size)` (the same path spliced into an inline `|T…|t` escape, with a required Blizzard fallback so it can never answer `nil`). Ends by calling `Media.RegisterLSM(addonName)` **at file load** — not at `PLAYER_LOGIN`, and not from `OnInitialize`, where this registration used to live: LibSharedMedia is vendored under `libs/` and has already run, while a deferred call opens a window in which a stored default names a face LSM has never heard of. `addonName` is the addon **folder** name, the first vararg — a different question from the frame-name prefix and from `## Title`, and the only one of the three that is a path component.
6. `core/Constants.lua` — exposes `NS.Constants` with the `SourceType` enum (stable string keys = the export contract; `Constants.lua:8-13`), `SourceOrder` / `SourceLabel`, the `SOURCE_IMPLEMENTED` mute-UI gate (`Constants.lua:37-41` — every source now has a live capture path, so all are offered), `ITEMCLASS_QUEST` (locale-independent quest-item class, `Constants.lua:44`), `CONTEXT_TTL` (1.5 s), the quality/retention/source option tables, and `FONT_MONO_NAME` / `FONT_MONO` — the latter resolved from `NS.MediaFont` **at file load**, which is why `core/MediaSetup.lua` sits above this file in the TOC. Publishes `NS.SourceType` / `NS.Confidence` aliases.
7. `core/Namespace.lua` — sets `NS.name`, `NS.version` (`"1.2.0"`, `Namespace.lua:5`), and `NS.PREFIX` (the cyan `|cff00ffff[LH]|r` chat tag, slash-commands-§4). Modules still self-publish idempotently; nothing else wired here.
8. `core/State.lua` — `NS.State`, the runtime-only mutable state: `lootContext` (the single-slot attribution stamp), `encounter` / `keystone` (instance context), the session flags `cleanupDone`, `debug`, and `testRecords`. Reset every load — never persisted to SavedVariables.
9. `core/Util.lua` — pure helpers: `PlayerKey` (`Name-Realm`), `SplitPath`, `FormatClock` / `FormatDate` / `FormatMoney` / `FormatBytes`, `RangeFrom` (date-range key → `from` epoch, used by the shared filter bar's Date dropdown), and `BuildLootPatterns` / `ParseSelfLoot` (compile the localized loot global-strings once and parse a `CHAT_MSG_LOOT` line into link + quantity). The shared **secret-safe chat printer** used to live here too; it is `LibKa0s-Core-1.0`'s now — see 10 below.
10. `core/CoreSetup.lua` — the **`LibKa0s-Core-1.0`** seam. Publishes `NS.Print` / `NS.Format` (the prefixed, secret-safe chat printer, built with `prefix = NS.PREFIX` and the default single-space separator), `NS.Util.print`, `NS.SafeToString` and `NS.IsConcatSafe`. Four load-order constraints pin it here and they are all real: **after** `core/Namespace.lua` (which defines `NS.PREFIX`, so the plain string form works and the function form is unnecessary), **after** `core/Util.lua` (which publishes `NS.Util`), **before** `core/LootHistory.lua` (whose AceConsole reclaim reads `NS.Util.print` back over `NS.Print` — publishing to BOTH keys is what makes that reclaim restore the library printer rather than silently undo the seam), and **before** the four files that capture `local print = NS.Print` at file scope (`modules/Browser.lua`, `settings/Schema.lua`, `settings/Slash.lua`, `settings/Panel.lua`). It also publishes **`NS.LIBKA0S_MISSING`**, the one cause clause every LibKa0s seam in this addon appends its own consequence to — a cross-file contract three other files depend on, set OUTSIDE the `if not lib` branch because they read it on both paths. It also publishes **`NS.ApplySkin`** — `Core.ApplySkin` on a working install, and its own pre-library copy of the same edge in the degraded branch — which is the seam `modules/Browser.lua`'s `B:ApplySkin` delegates to, so the window edge is `Core.SKIN`'s and is not restated in this addon (standalone-windows). It now also publishes **`NS.MakeCloseButton`** — `lib.MakeCloseButton(parent, onClick, addonName)`, wrapped **once** here so the addon **folder** name reaches the library from every close control this addon builds (the History title bar and the export modal, both through `modules/Browser.lua`'s `B:MakeCloseButton`; the export copy window's close is the same control, reached by `LibKa0s-Widgets-1.0`'s `CopyWindow` calling `Core.MakeCloseButton` with the addon name itself). The third argument is the whole point of the wrapper: without it Core draws a multiplication sign, and a two-argument passthrough onto a three-argument target is anti-patterns #64 — it runs, it returns a button, and every suite stays green. `tests/test_libka0s.lua` asserts the argument by name. The old decline of `Core.MakeCloseButton` (closed issues [#19](https://github.com/tusharsaxena/LootHistory/issues/19), [#25](https://github.com/tusharsaxena/LootHistory/issues/25), [#26](https://github.com/tusharsaxena/LootHistory/issues/26)) is retired: it rested on Core's close being a thin 18×18 ×, and since Core minor 6 it is this collection's `close` mark. The 24×24 class-colored glyph survives only in the degraded branch, where there is no library to ask.
11. `core/WidgetsSetup.lua` — the **`LibKa0s-Widgets-1.0`** seam, and the file that let `modules/Browser.lua` lose 208 lines: a `MakeDropdown` factory, a `FULLSCREEN_DIALOG` singleton popup with pooled rows, a full-screen click-catcher and six helpers, all of them the collection's third copy of one widget. Publishes **`NS.MakeDropdown(parent, width)`** — one factory for all ten instances (the filter bar's nine and the export modal's Data Set picker), resolving the library's `chevron` and `check` through `NS.Icon` **here** rather than at each call site, because a vendored library cannot know which addon folder it sits in and so takes every piece of art as a parameter — plus **`NS.HasWidgets()`**, the same availability question answered with nothing built (a surface whose only control is a dropdown must be able to refuse *before* creating a globally-named frame), and **`NS.CloseMenu()`**. It passes **no `glyphFont`**, deliberately: that field is a *precondition* for any option carrying `opt.glyph`, no option this addon builds carries one, and a proportional face handed over for a glyph nothing draws renders a box. Loads after `core/MediaSetup.lua` (whose `NS.Icon` it reads) and before `modules/`. `nil` is a real answer: `NS.MakeDropdown` returns nil with no library and both surfaces **refuse to draw** rather than build a dead control, while `NS.CloseMenu` becomes a no-op. It also publishes **`NS.CopyWindow(descriptor)`** — `Widgets.CopyWindow`, the read-only Ctrl+C window `modules/Export.lua` shows its CSV in. A HANDLE, not a frame: nothing is built until the first `:Show(text)`, so a session that never exports creates nothing, and a rebuild per open would leak one frame per export because frames are never destroyed in WoW. It answers nil with no library, which nothing downstream has to handle separately — the modal that would have shown it already refuses to open.
12. `core/DebugLogSetup.lua` — the **`LibKa0s-DebugLog-1.0`** seam, replacing the 359-line `modules/DebugLog.lua` outright. Publishes `NS.DebugLog` (the console window, the copy window, both formatters, the buffer and the enable seam) and rebinds the global sink `NS.Debug`. Loads after `core/Constants.lua` (the descriptor reads `FONT_MONO` at `:New` time) and before `core/Database.lua` (whose migrations call `NS.Debug` during `InitDB`). The window *edge* stays this addon's through the `applySkin` descriptor hook, passed as a **closure** that resolves `NS.Browser` at frame-build time — hoisting that lookup into a local here would silently lose the skin, because `modules/Browser.lua` loads much later. The descriptor carries **`addonName = addonName` beside `name = addonName`** — the same string answering two different questions: `name` seeds the frame globals, `addonName` is what the library builds a **texture path** from. That one line is what turns the console's title strip from a multiplication sign and the words "Copy"/"Clear" into three icon controls, on both the console and its copy window. **`makeCloseButton` is still deliberately not passed**: the close control on a library-drawn window is the library's — and since Core minor 6 the library's *is* this collection's `close` mark, so the two windows now match without either side overriding the other (standalone-windows; closed issue [LIBKA0S-19](https://github.com/tusharsaxena/LootHistory/issues/26), asserted in `tests/test_debuglog.lua`).
13. `core/LootHistory.lua` — the AceAddon bootstrap. `AceAddon:NewAddon(NS, addonName, "AceEvent-3.0", "AceTimer-3.0", "AceConsole-3.0")` promotes `NS` in place (no `_G.LootHistory` rebind — the namespace stays private) and sets `NS.addon` / `NS.bus`. Defines `NS.NewBusTarget()` — the fresh-AceEvent-target factory every message consumer must use so two consumers can't clobber each other on the shared bus (`LootHistory.lua:20-26`; see [message-bus.md](message-bus.md)). Defines the three lifecycle hooks (below).
14. `core/Database.lua` — defines `NS:InitDB` (`AceDB-3.0:New("LootHistoryDB", NS.defaults, true)` — account-wide) and `NS:RunMigrations` (reads/writes `db.global.schemaVersion` and walks the module-level `MIGRATIONS` array, `Database.lua:19-117`, whose v1→v2 entry strips the retired `viaWhitelist` field, `Database.lua:23-29`), then `NS.Database` with the read/write path: `History` (the raw array) / `ActiveHistory` (the test-mode-aware read seam behind Query/Stats — blacklist/whitelist filtering is point-in-time, decided at capture, so reads never hide a stored row), `Count`, `Add` (fires `RecordAdded`), `QueryList` / `Query`, `Export` (the v2 export contract), `Stats` (the single O(n) pass all Insights widgets consume), `Delete` / `Purge` / `PruneOld` (all fire `HistoryChanged`), `RepairBoundStates` (the deferred warbound-state split, armed by `NS:ArmBoundRepair`), and `StorageStats`. Does not create the DB at file-load.
15. `defaults/Global.lua` — `NS.defaults.global`: `schemaVersion = 1`, `history = {}`, `blacklist = {}` / `whitelist = {}` (the item-id filter lists), `currencyBlacklist = {}` (the currencyID-keyed list), the `settings` table (enabled, qualityThreshold, excludeQuestItems, recordCurrency, excludedSources, retentionDays, windowScale, window, and the `auction` cascade), and `minimap = { hide = false }`. History, lists and settings all live under `.global` (account-wide); debug is deliberately NOT here (session-only). See [schema.md](schema.md).
16. `modules/Attribution.lua` — **loads before Filters/Collector.** The source-resolution engine (`NS.Attribution`). `Stamp` writes the single-slot `State.lootContext` (fresh for `CONTEXT_TTL`, not cleared on consume — one loot window emits many lines); `Consume` reads it or falls back to `OTHER`/`INFERRED`. `ResolveLootSource` maps a loot-slot GUID + instance state → source + detail (Creature→KILL, GameObject→MPLUS/CONTAINER, Item→CONTAINER). `Attribution:Enable` (called from `OnEnable`, never at file-load) registers the peripheral stampers: LOOT_OPENED, ENCOUNTER/CHALLENGE_MODE, TRADE_ACCEPT_UPDATE, QUEST_TURNED_IN, a player-only `RegisterUnitEvent` frame for deconstruct spell-success, and `hooksecurefunc` hooks for vendor buy / mail take / container use / quest reward. See [data-flow.md](data-flow.md).
17. `modules/Filters.lua` — `NS.Filters`, the blacklist/whitelist item-id lists (issue #14) **plus the currencyID-keyed currency blacklist**. **Loads before Collector.** Copy-on-write `AddBlacklist` / `AddWhitelist` (each drops the id from the sibling list) / `RemoveBlacklist` / `RemoveWhitelist` write a fresh table to `NS.db.global.{blacklist,whitelist}` and re-cache the Collector + fire `HistoryChanged` (the browser re-queries); `AddCurrencyBlacklist` / `RemoveCurrencyBlacklist` (blacklist-only, no sibling) manage `NS.db.global.currencyBlacklist`; `Blacklist` / `Whitelist` / `CurrencyBlacklist` (the live id sets) / `SortedIDs` / `ParseItemID` / `ParseCurrencyID` (id from a number, link, or itemString) support the panel + right-click UIs. `ClearList` / `ClearAll` empty one or all lists (the Filters page's per-list **Clear all** and header **Defaults** buttons; `ClearAll` includes the currency list). Data-only — no `Enable`. An architecture-§5 carve-out from `Schema:Set`.
18. `modules/AuctionPrice.lua` — `NS.AuctionPrice`, the AH-price integration boundary. **Loads before Collector.** `GatherAll(itemLink, itemID)` resolves the live `settings.auction.capture` set into a per-provider fetch (Auctionator / TSM / OribosExchange), capturing **every** configured key — not just one — into a nested `provider → key → copper` map. Each provider fetcher is `pcall`-wrapped so a broken/absent addon degrades to `nil` and the gather continues to the next. `Pick(map)` is the separate read-time seam: it walks `settings.auction.priority` (an ordered cascade selection list) and returns `price, tag` for the first present key (both `nil` when the map is empty or nothing captured). Data-only — no `Enable`. Deliberately **outside** `core/Compat.lua` (Blizzard-API-only) — see Standards compliance in [ARCHITECTURE.md](ARCHITECTURE.md).
19. `modules/Collector.lua` — `NS.Collector`, the acquisition path. `ShouldRecord` is the pure, point-in-time gate: a blacklisted id is an absolute veto (the item is never written); otherwise the item records if it passes the normal gate (quality / excluded-source / quest-item, keyed on the locale-independent `ITEMCLASS_QUEST`), or — failing that — if it is whitelisted, in which case it returns `true, "whitelist"` and the row is written as a plain record with no marker of how it got in (later whitelist edits never revisit it). `BuildRecord` assembles the record, including the `auctionPrice` map stamped by an `AuctionPrice:GatherAll` call. `OnChatMsgLoot` parses the self-loot line, consumes the attribution context, gates (passing the item id + the cached lists), gathers item/zone extras, gathers the AH prices, and calls `Database:Add`. `OnChatMsgCurrency` handles `CHAT_MSG_CURRENCY` with a slimmer gate — the `recordCurrency` master toggle, the per-source mute list, and the currency blacklist (no quality/quest/itemID checks) — writing a `Type=Currency` row that reuses the same attribution context (a self-identifying "You are refunded" currency line attributes REFUND directly). `Collector:Enable` registers `CHAT_MSG_LOOT` + `CHAT_MSG_CURRENCY` and — on its own `NS.NewBusTarget()` — `Ka0s_LootHistory_SettingsChanged`, refreshing the hot-path upvalues (`enabled`/`qualityThreshold`/`excludedSources`/`excludeQuestItems`/`recordCurrency`/`blacklist`/`whitelist`/`currencyBlacklist`) rather than reading the DB per loot (Ka0s events-frames-taint-§7).
20. `modules/Browser.lua` — `NS.Browser`, the standalone window (Ka0s standalone-windows reference implementation). Owns the non-secure `LootHistoryWindow` frame (HIGH strata, movable/resizable, ESC via `UISpecialFrames`), the `ApplySkin` re-skin seam (which delegates the edge to `NS.ApplySkin`/`Core.SKIN`; the local `SKIN` table keeps only the tab colors and layout heights), window position/scale persistence to `NS.db.global.settings.window` (an architecture-§5 carve-out from `Schema:Set`), the History/Insights tab strip (lazy pane build), the **shared singleton filter bar + footer** (multi-select dropdowns Bound / Quality / Type / SubType / Source / Zone / Character; `CurrentFilter` feeds both the table and the Insights charts — issue #13), the saved-view logic and session-only current-player character scope, the **tab-aware `Export` button** (`OpenExport` → loot rows on History, analytics on Insights — issue #15), and the LibDataBroker launcher + LibDBIcon minimap button. **Depends on `LibKa0s-Widgets-1.0`, reached through `core/WidgetsSetup.lua`'s `NS.MakeDropdown` / `NS.CloseMenu` — never `LibStub` directly.** Its nine dropdowns are the library's; the first one built is the probe, and with no library `BuildFilterBar` returns before publishing `B._dd`, so the bar is absent rather than dead. `NS.CloseMenu()` is called from the window's `OnHide` (which is also the Escape / `UISpecialFrames` route) and from `B:Hide` (the slash-command close), because the popup is a process-wide singleton at `FULLSCREEN_DIALOG` that this frame's own `Hide()` cannot reach. Exposes `GetWindow` for `Export` to anchor to; it no longer exposes a dropdown factory of its own. `Browser:Enable` subscribes to `SettingsChanged` / `HistoryChanged` / `RecordAdded` on its own bus target and calls `SetupMinimap`. See [browser.md](browser.md).
21. `modules/BrowserTable.lua` — `NS.BrowserTable`, the virtualized pooled-row History table. Defines the `COLUMNS` model (Character always last; SubType after Type), the filter→sort→group→slice→bind pipeline over `Database:QueryList(CurrentRecords, filter)`, `OrderedFilteredRecords` (the on-screen order the Export Current-View dataset serializes), a `FauxScrollFrame` + row pool (never one frame per record, Ka0s standalone-windows), collapsible group headers, the class-icon / bound-lock markup, the right-click row menu (link / **blacklist** / delete), and the deterministic-PRNG synthetic dataset for `/lh test` (`ToggleTestMode` publishes `NS.State.testRecords`, so both the table and Insights render off the same fake data).
22. `modules/Export.lua` — `NS.Export`, the export modal (`Export:Open`). Builds a small skinned window with a Data Set dropdown (All Data / Current View) and an Export-to-CSV button, centering on the History window. **Depends on `LibKa0s-Widgets-1.0`, reached through `core/WidgetsSetup.lua`'s `NS.MakeDropdown` / `NS.HasWidgets` / `NS.CloseMenu` — directly, not through `NS.Browser`, which no longer owns a widget.** With no library the modal **refuses to open** and says why through `NS.LIBKA0S_MISSING`, rather than calling `SetOptions` on nil — and the refusal is decided by `NS.HasWidgets()` at the top of `EnsureFrame`, before `CreateFrame`, because this window carries the global name `LootHistoryExportWindow` and probing it by building the picker and discarding the frame stranded one such frame per `:Open` call. The picker is still built before any other child, so a seam that answered nil despite the library having registered costs one bare frame rather than a half-built modal. Its `OnHide` calls `NS.CloseMenu()`, covering both the title-bar close and Escape — before the adoption it had no `OnHide` at all, and either route left the Data Set menu orphaned over the game. Still reuses `Browser:ApplySkin` / `MakeCloseButton` for the modal. Driven by a per-open `config = { title, providers, csv }` — the invoking tab supplies the window header ("Export History" / "Export Insights"): `CSV` serializes loot rows (record fields + derived `date`/`time`/human `quality`/`vendorPrice` beside `*Raw` columns, the computed `auctionPrice`/`auctionPriceRaw`/`value`/`valueRaw`/`auctionSource` columns from `NS.AuctionPrice:Pick` + `NS.Util.RecordValue`, one raw `auc_<provider>_<key>` column per `NS.Constants.AUCTION_KEYS` entry, + `wowheadLink` last) for the History tab; `InsightsCSV` serializes a `Database:Stats` result into a sectioned analytics CSV for the Insights tab (issue #15). `WowheadLink` builds a `wowhead.com/item=…?bonus=…` URL from the item's bonus IDs; output opens in a **`LibKa0s-Widgets-1.0`** `CopyWindow`, leased through `core/WidgetsSetup.lua`'s `NS.CopyWindow`: this file passes a descriptor (global name, mono face, title, skin, anchor) and builds no frame of its own. `width` / `height` / `editWidth` / `backdrop` are deliberately not passed, because the library's defaults are this addon's old constants (640x420, 590, 0.06/0.06/0.08/0.95). The handle is lazy, so a session that never exports creates nothing; `Export.__copyWindow` / `Export.__showCopy` publish it to the suite, an EditBox being write-only through the frame API as this module uses it. Called directly by the Browser — no bus message.
23. `modules/Analytics.lua` — `NS.Analytics`, the Insights tab, split by two dividers into a **LOOT** block and a **CURRENCY** block. Attaches to the Browser's Insights pane, builds stat/highlight cards + the **LOOT** breakdowns (items-only: source, value, quality, item type, bound type, each with a per-character stacked companion; per-day + value-per-day strips; hour-of-day; weekday; top zones/items/value lists) and the **CURRENCY** breakdowns (Currency Collected, Currency by Type × Source, Currency by Character × Type, currency-per-day — shown only when the range has currency events), all off a single `Database:Stats(filter)` pass **scoped by the browser's shared filter** (`Browser:CurrentFilter`, issue #13 — no range selector of its own). `byKeystone`/`byConfidence`/`currencyBySource` are still computed but feed only the Export (their in-dashboard charts were removed). Widgets are pooled and re-laid-out on resize; `BuildCharts` subscribes to `RecordAdded` / `HistoryChanged` on its own bus target for live refresh.
24. `settings/Schema.lua` — `NS.Schema` (alias `S`): one row per setting (`S.Schema`, `Schema.lua:23-113`) driving AceDB defaults, panel widgets, and the slash CLI. `S:Set(path, value)` is the single user-setting write seam — validate → deep-copy → write to `NS.db.global` → `onChange` (`Schema.lua:160-175`); the deep-copy stops a reset from aliasing the DB to a shared default table. Also owns `NS.COMMANDS`, the slash dispatch/help table (`Schema.lua:213-245`). `debug` is intentionally not a Schema row. Settings load **after** modules — nothing in `modules/` reads `NS.Schema`/`NS.COMMANDS` at file-load, only inside functions at runtime.
25. `settings/Slash.lua` — `NS.Slash`, and the **`LibKa0s-Slash-1.0`** seam. Registers `/lh` and `/loothistory` via AceConsole and hands the library the positional `NS.COMMANDS` triples; the dispatcher, the help header and rows, `LandingRows`, `BuildListLines`, `CliGet` / `CliSet` / `CliList` / `CliReset` / `CliResetAll` / `CliVersion` and the type-aware value parser are all the library's. What stays this addon's: the purge / reset-everything / list-clear `StaticPopupDialogs` confirm dialogs registered at file-load, the `ResetEverything` full wipe behind the panel's **Reset Everything** button (deliberately not called "Reset All", which is what the lesser `/lh resetall` verb does), the `CliResetAll` wrapper that also clears the three id-lists (a storage carve-out with no schema row, so the library's row walk cannot see them), and `Sl.FormatSchemaValue` — passed as the descriptor's `format` hook, holding the one branch the library has no type for (`type = "table"`, which would otherwise fall through to Core's `SafeToString` and render a plain settings value as `<secret>`). See [slash-dispatch.md](slash-dispatch.md).
26. `settings/OptionsSetup.lua` — the **`LibKa0s-Options-1.0`** seam, and where every declined surface is written down. Publishes `NS.Options`: the Blizzard canvas shell, the breadcrumb header, the lazy Defaults button, the page registry, `EnsureScroll` plus the always-shown scrollbar patch, `Section` / `AddSpacer` / `AttachTooltip`, the five widget makers, the two-column flow engine, `SetRenderer`, the two refresh tiers and `OpenOptionsPanel`. The descriptor points `get` / `set` / `applyDefault` at `NS.Schema`, maps `rowsForPage` onto the schema's `group` (this addon's section headings *are* its pages), stashes AceGUI as `NS.AceGUI` for the page file, and late-binds `buildMain` through `NS.Panel.BuildMain`. Must load **before** `settings/Panel.lua`, which takes `NS.Options` as a file-scope upvalue.
27. `settings/Panel.lua` — `NS.Panel`. Registers the parent `Settings.RegisterCanvasLayoutCategory` (a logo + tagline + slash-command landing page) plus three subcategories: `General` (the schema settings), `Filters` (blacklist/whitelist item-id management — a custom non-schema page: add-by-id/link box + a live list with Remove per id, driven by `NS.Filters`), and `AH Price` (the master `enabled` schema toggle + a **frame-light unified price table**, `buildAuctionTable`: FontString text columns + reused row slots so it stays ~50 frames — the previous ~213-frame page froze the Settings canvas on tab-transition; per-row Enabled checkbox writes the single collect-and-rank `settings.auction.capture` flag, reorder arrows drive `settings.auction.priority`). The canvas shell, the breadcrumb header, the lazy Defaults button, the scroll + always-shown-scrollbar patch (Ka0s options-ui-§10), the section headings and the two-column flow engine are all `LibKa0s-Options-1.0`'s now (see 24); this file supplies the page builders, the live History stats + Purge section, and the two widgets the library has no maker for. Three of the four pages declare themselves through `O.SetRenderer`, which owns *when* they draw — first show, and again after a structural refresh marked them dirty while hidden. Structural refreshes are split from scalar ones (options-ui-§11): scalar widgets re-sync in place every `OnShow`, structural list rebuilds are gated to first-paint / on-screen edit / a dirty flag. Two surfaces are deliberately host-drawn: the inverted muted-source picker (none of the library's five makers, emitted from `afterGroup` so it lands exactly where the generic path used to put it) and the AH Price page's own `OnShow` (its eleven pooled row slots of raw FontStrings are parented to an AceGUI SimpleGroup's frame, and a renderer's `ClearScroll` would release that group and orphan them). Both are recorded in closed issues [LIBKA0S-14](https://github.com/tusharsaxena/LootHistory/issues/20) and [LIBKA0S-15](https://github.com/tusharsaxena/LootHistory/issues/21). Each subcategory carries a header **Defaults** button (options-ui-§5), built lazily on that panel's first `OnShow` so the shared AceGUI widget is created after skinning addons have hooked `RegisterAsWidget` — never at registration time: General's runs the non-destructive `CliResetAll` (and recenters the window); Filters' clears both id-lists (confirm-gated → `NS.Filters:ClearAll`); AH Price's resets its schema rows + priority order. Writes route through `NS.Schema:Set` (schema rows) or `NS.Filters` (the lists); `P:Open` is combat-gated. See [settings-panel.md](settings-panel.md).

## AceAddon lifecycle

1. **TOC file-load** as listed above. After load: `NS.Compat` / `NS.Constants` / `NS.State` / `NS.Util` are populated; `core/LootHistory.lua` has run `AceAddon-3.0:NewAddon` to promote `NS` in place (setting `NS.addon` / `NS.bus` and defining `NS.NewBusTarget`); `NS.defaults` is set; `NS.L` exists; `NS.Schema` (with `NS.COMMANDS`), `NS.Slash` (with its confirm dialogs registered) and `NS.Panel` exist; each module has published `NS.X = NS.X or {}` and defined its methods but **not** enabled — no module touches a WoW event/hook/frame API at file-load (so the headless test harness can load every file).
2. **`addon:OnInitialize`** (Ace lifecycle, fires on `ADDON_LOADED`; `LootHistory.lua:28-35`): `NS:InitDB` builds the AceDB instance and runs `NS:RunMigrations` (the idempotent seam; the `MIGRATIONS` array ships seven steps carrying the stamp from the seeded `1` to the current shape `8` — see [schema.md](schema.md)); `NS.Schema:Register` runs boot validation (every schema path must resolve against the defaults); `NS.Slash:Register` binds `/lh` + `/loothistory`; `NS.Panel:Register` registers the Blizzard canvas categories (deferred content — panels render lazily on first `OnShow`).
3. **`addon:OnEnable`** (`LootHistory.lua:37-45`): registers `PLAYER_ENTERING_WORLD` → `OnEnterWorld`, then enables the modules in order — `Attribution:Enable` (peripheral event registrations + read-side hooks), `Collector:Enable` (`CHAT_MSG_LOOT` + the `SettingsChanged` upvalue-refresh subscription), and `Browser:Enable` (bus subscriptions + `SetupMinimap`). `Filters` is data-only (no `Enable`); `BrowserTable` and `Analytics` have **no** `Enable` — they attach lazily when the Browser first builds their pane (`Analytics` subscribes to the bus in `BuildCharts`); `DebugLog` likewise builds its frame lazily. `Panel` and `Slash` do their work in `OnInitialize`, not here.
4. **`addon:OnEnterWorld`** (`LootHistory.lua:48-62`): guarded to run once per session (`NS.State.cleanupDone`). Schedules `Database:PruneOld` via `C_Timer.After(5, …)` so retention cleanup lands 5 s after the login/zone spike rather than during it, and rides the same deferral for `Database:RepairBoundStates` — which runs again at +20 s, because it needs an item cache that is cold at login and the first pass is what warms it. `PruneOld` drops records older than `settings.retentionDays` (0 = keep Always) by rebuild-and-swap and fires `HistoryChanged`.

## External dependencies

All vendored under `libs/` and pulled in by `LootHistory.toc` (Ka0s Standard v2.0.0 — vendoring is the suite-wide rule; do not switch to `.pkgmeta` externals): LibStub, CallbackHandler-1.0, AceAddon-3.0, AceEvent-3.0, AceTimer-3.0, AceConsole-3.0, AceDB-3.0, AceGUI-3.0, LibSharedMedia-3.0, LibDataBroker-1.1, LibDBIcon-1.0, and **[LibKa0s](https://github.com/tusharsaxena/LibKa0s)** — the Ka0s-owned shared library, of which this addon wires seven majors (`Core`, `Media`, `DebugLog`, `Slash`, `Options`, `Widgets` and — new at v1.15.0 — `Env`) and declines `Perf` (see closed issue [LIBKA0S-17](https://github.com/tusharsaxena/LootHistory/issues/22)); `Pool` and `Item` are not wired yet. LibKa0s is vendored **whole-folder**, never file by file: nine of its ten majors resolve `LibKa0s-Core-1.0` before registering and refuse outright against an older minor, so a per-file copy is how cross-major skew gets manufactured. `tests/_kit/` is the same library's test harness, vendored under `tests/` (never `libs/`, which is the ship payload) — see [testing.md](testing.md#the-vendor-gate) for the four diffs that keep both copies honest. LibSerialize / LibDeflate are **not** vendored and there is no plan to add them — export is CSV-only (see [scope.md](scope.md)), so nothing needs a serialized/compressed share string. The `## OptionalDeps` line lists the same set so a shared standalone copy is used when present. `LootHistory.toc`'s `## Interface: 120007` targets Midnight (12.0.7).
